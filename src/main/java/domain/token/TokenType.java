package domain.token;

public enum TokenType {
    SET, WORD, TEXT, NUMBER,
    PLUS, MINUS, STAR, DIVIDE, EQ, LT, GT, EQEQ, LTEQ, GTEQ, EXCL, EXCLEQ, BAR, BARBAR, AMP, AMPAMP,
    LPAREN, RPAREN, LBRACE, RBRACE, COMMA,
    PRINT, IF, ELSE, WHILE, FOR, DO, BREAK, CONTINUE, DEF, RETURN,
    EOF
}
